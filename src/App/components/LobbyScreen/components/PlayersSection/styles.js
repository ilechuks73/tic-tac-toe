import {
  makeStyles
} from "@material-ui/styles";

export const useStyles = makeStyles({
  PlayersSection: {
    borderRadius: "10px",
    border: "1px solid grey",
    padding: "5px 10px",
    marginBottom: "10px"
  }
})